<div>
    <h1>Todos los Productos</h1>
    <div>
         {{#each products}}
            <div>
                <h2>{{this.title}}</h2>
                <p>$ {{this.price}}</p>
                <p>Codigo: {{this.code}}</p>
                <p>Stock: {{this.stock}}</p>
            </div>
        {{/each}}
    </div>
</div>